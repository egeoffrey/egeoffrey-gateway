branch: development
container_config:
  ports:
  - 443:443
  - 1883:1883
  volumes:
  - ./data/gateway:/config
description: Runs the eGeoffrey Gateway through which all the modules can exchange
  information
dockerhub: egeoffrey/egeoffrey-gateway
github: egeoffrey/egeoffrey-gateway
icon: project-diagram
manifest_schema: 2
modules: []
package: egeoffrey-gateway
revision: 5
tags: controller
version: 1.0
