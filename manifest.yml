manifest_schema: 1
package: myhouse-gateway
revision: 2
version: development
github: myhouse-project/myhouse-gateway
dockerhub: myhouseproject/myhouse-gateway
container_config: 
  ports:
    - "443:443"
    - "1883:1883"
  volumes:
    - ./data/gateway:/config
description: Runs the myHouse Gateway through which all the modules can exchange information
tags: controller
modules: []

